/**
 * Watchtower data layer.
 *
 * Phase 1 adds Prisma here: schema (Owner, Watch, Notification), the generated
 * client, and a singleton export the Next.js backend imports. Neon Postgres is
 * the target. Placeholder for the Phase 0 scaffold.
 */
export const DB_PACKAGE = "@watchtower/db" as const;
